读取开关基线、变更队列、租户政策、审批表和发布合同，建立可按业务键定位的输入关系。
按release_contract.json规定的顺序检查开关、审批、语义版本和租户有效性。
把每条变更展开到目标租户，在独立配置副本上执行JSON Patch。
核对补丁后的ramp与租户上限，只把通过裁决的配置纳入发布状态。
记录每个change_id与tenant_id组合的裁决、原因、结果值和配置路径。
生成租户配置文件与tenant_release_matrix.csv，逐项核对矩阵主键和文件路径。
汇总通过数、拒绝数、原因分布、发布租户和配置清单，形成release_summary.json。
运行npm run release并复核决策账、矩阵、摘要与配置内容之间的对应关系。
